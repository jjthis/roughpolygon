#include "testlib.h"
 
using namespace std;
 
const long long NLIM = 200'000;
 
int main(int argc, char *argv[]) {
    registerValidation(argc, argv);
 
    auto N = inf.readLong(1, 5ll, "N");
    inf.readEof();
 
    return 0;
}
